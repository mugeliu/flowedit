# OpenAI API Configuration
# Supports OpenAI and OpenAI-compatible APIs (e.g., Moonshot, DeepSeek, GLM, etc.)
OPENAI_API_KEY=your_api_key_here
OPENAI_MODEL=gpt-4
# Optional: Custom API endpoint for OpenAI-compatible services
# Examples:
#   OpenAI: https://api.openai.com/v1 (default, can be omitted)
#   Moonshot/Kimi: https://api.moonshot.cn/v1
#   DeepSeek: https://api.deepseek.com
#   GLM: https://open.bigmodel.cn/api/paas/v4
OPENAI_BASE_URL=

# Multi-Model Strategy Configuration (Optional)
# If not specified, will use OPENAI_MODEL for all tasks
# Recommended setup for cost optimization:

# Fast model for simple tasks (feedback generation, validation)
# Examples: gpt-3.5-turbo, gpt-4o-mini
FAST_MODEL=gpt-3.5-turbo

# Complex model for sophisticated tasks (analysis, design, engineering)
# Examples: gpt-4, gpt-4-turbo, claude-3-sonnet
COMPLEX_MODEL=gpt-4

# Preprocessing model for content cleaning and HTML parsing
# Examples: gpt-4o-mini, gpt-3.5-turbo
PREPROCESSING_MODEL=gpt-4o-mini

# Validation model for output verification and quality checks
# Examples: gpt-3.5-turbo, gpt-4o-mini
VALIDATION_MODEL=gpt-3.5-turbo

# Model Configuration
MAX_TOKENS=4000
TEMPERATURE=0.7

# Database Configuration
DATABASE_URL=sqlite:///./style_flow.db

# Application Configuration
DEBUG=True
LOG_LEVEL=INFO

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
