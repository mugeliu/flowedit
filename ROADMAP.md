# ROADMAP

## 当前阶段

- 阶段：FlowEdit 单一 HTML 插入能力收缩。
- 当前目标：只保留粘贴 HTML、实时预览和插入微信公众号编辑器。
- 当前状态：代码改造和本地自动化验证已完成，待真实微信公众号页面与视觉验收。

## 本阶段范围

- 在微信工具栏提供单一 `FlowEdit` 入口。
- 左侧粘贴 HTML，右侧在无脚本沙箱中实时预览。
- 校验 HTML 后一次性插入微信编辑器。
- 拒绝高风险标签、事件属性、JavaScript 地址和本地图片地址。
- 移除文章文件夹协议、Markdown 渲染、本地图片处理上传及对应界面和资源。

## 已完成

- 已更新 `AGENTS.md` 和 `CLAUDE.md`，将项目边界收缩为单一 HTML 插入能力。
- 已移除顶部“HTML 插入／文件夹导入”切换栏、文件夹选择区和手动预览按钮。
- 已将弹层重写为左右双栏布局，左侧粘贴 HTML，右侧使用 120 毫秒防抖自动刷新沙箱预览。
- 已在 HTML 为空或校验失败时清空预览并禁用插入按钮，插入前会再次校验。
- 已删除文章文件协议、Markdown 渲染、固定渲染素材、本地图片压缩上传及微信上传凭证读取代码。
- 已同步更新 `README.md`、`manifest.json`、`package.json` 和 Vite 构建配置。
- 已在 `README.md` 说明预览沙箱与微信编辑器 JSAPI 的独立链路，以及脚本拦截日志的安全含义。
- 4 项自动化测试全部通过，TypeScript 类型检查和 Vite 生产构建通过。

## 待办

- 在真实微信公众号编辑页验收弹层的桌面宽屏布局。
- 验收粘贴 HTML 后右侧实时预览。
- 验收安全 HTML 能完整插入微信编辑器。
- 验收危险 HTML 被拒绝，插入失败时编辑器正文保持不变。
- 验收窄窗口下上下分栏的响应式布局。

## 阻塞

- 当前自动化会话没有可用的本地测试浏览器实例，无法完成视觉截图验收。
- 真实微信页面验收需要可用的 Chrome 实例和有效的微信公众号登录状态。

## 最近验证

- 2026-08-12：`npm run typecheck` 通过。
- 2026-08-12：4 项 Node 自动化测试全部通过，覆盖 HTML 安全校验和微信编辑器页面桥接。
- 2026-08-12：`npm run build` 通过，生成 `dist/content/content.js`、页面桥接脚本、扩展清单和图标。
- 2026-08-12：源码和构建产物未发现文件夹导入、Markdown 渲染、图片上传或固定渲染素材残留。
