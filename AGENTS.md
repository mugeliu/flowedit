# FlowEdit 维护规则

## 项目目标

FlowEdit 是一个只运行在微信公众号文章编辑页的 Chrome 扩展，仅保留一项能力：接收用户粘贴的 HTML，实时预览并插入微信编辑器。

插件不读取本地 HTML 文件或文章文件夹，不处理 Markdown，不上传本地正文图片，也不提供飞书文件夹同步。

## HTML 插入规则

- 左侧粘贴 HTML 源码，右侧在无脚本沙箱中实时预览。
- HTML 内容必须非空；插入前必须再次校验。
- 拒绝脚本、高风险标签、事件属性、JavaScript 地址和本地图片地址。
- 图片必须使用微信可访问的远程 URL，禁止 `blob:`、`data:`、`file:` 和相对路径。
- 校验或插入失败时，不得向微信编辑器插入残缺正文。

## 工程边界

- 核心代码统一使用 TypeScript。
- 不使用 Editor.js、React、Tailwind、shadcn/ui、WeUI、模板系统、文章历史或插件注册系统。
- 不保留本地文章协议、Markdown 渲染、图片压缩上传或后台 AI 工作流。
- 不生成 `preview.html`、`article.json` 或 `wechat-draft.json`。
- 不新增飞书权限、飞书密钥或同步代码。
- 不在代码、日志、存储或构建产物中保存微信票据、token 或其他凭证。
- 微信页面协议变更必须先对照可运行参考实现或当前工作版本，不靠猜测反复试错。

## 修改与验证

- 只修改与上述 HTML 插入能力直接相关的文件。
- 每次完成代码改动后运行类型检查、自动化测试和完整构建。
- 真实微信页面验收至少覆盖：HTML 实时预览、纯 HTML 插入、危险 HTML 拒绝和失败时不插入残缺正文。
- 只有已经实现并验证的事项才能写入 `ROADMAP.md`“已完成”。
